<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="amenityCharge.create">
	    <![CDATA[
			INSERT INTO AmenityCharge (ProviderId, AmenityId, Charge, CreatedBy, CreatedOn, UpdatedBy, UpdatedOn) 
			VALUES (?, ?, ?, ?, ?, ?, ?)
	    ]]>
	</entry>
	
	<entry key="amenityCharge.update">
	    <![CDATA[
			UPDATE AmenityCharge SET 
				Charge = ?,
				UpdatedBy = ?, 
				UpdatedOn = ? 
			WHERE 
				AmenityId = ? AND ProviderId = ?
	    ]]>
	</entry>
	
	<entry key="amenityCharge.delete">
	    <![CDATA[
			DELETE FROM AmenityCharge WHERE Id = ?
	    ]]>
	</entry>
	
	<entry key="amenityCharge.getAmenityChargeById">
	    <![CDATA[
			SELECT 
				AmenityCharge.Id, 
				AmenityCharge.ProviderId, 
				AmenityCharge.AmenityId, 
				AmenityCharge.Charge,
				Amenity.Name
			FROM AmenityCharge 
			LEFT OUTER JOIN Amenity ON 
				Amenity.Id = AmenityCharge.AmenityId 
			WHERE 
				AmenityCharge.Id = ?
	    ]]>
	</entry> 
	
	<entry key="amenityCharge.getAllAmenityChargesByProviderId">
	    <![CDATA[
			SELECT 
				AmenityCharge.Id, 
				AmenityCharge.ProviderId, 
				AmenityCharge.AmenityId, 
				AmenityCharge.Charge,
				Amenity.Name
			FROM AmenityCharge 
			LEFT OUTER JOIN Amenity ON 
				Amenity.Id = AmenityCharge.AmenityId 
			WHERE 
				AmenityCharge.ProviderId = ?
			ORDER BY AmenityCharge.Id DESC
	    ]]>
	</entry>
	
	<entry key="amenityCharge.isAvailable">
	    <![CDATA[
			SELECT
				Id
			FROM AmenityCharge
			WHERE
				(? > 0 AND Id != ? AND AmenityId = ? AND ProviderId = ?)
			    OR (? <= 0 AND AmenityId = ? AND ProviderId = ?) 
	    ]]>
	</entry> 
</properties>