<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="itemAmenity.create">
	    <![CDATA[
			INSERT INTO ItemAmenity (ItemId, AmenityId, Quantity, CreatedBy, CreatedOn, UpdatedBy, UpdatedOn) 
			VALUES (?, ?, ?, ?, ?, ?, ?)
	    ]]>
	</entry>
	
	<entry key="itemAmenity.update">
	    <![CDATA[
			UPDATE ItemAmenity SET 
				ItemId = ?, 
				AmenityId = ?,
				Quantity = ?,
				UpdatedBy = ?, 
				UpdatedOn = ? 
			WHERE 
				Id = ?
	    ]]>
	</entry>
	
	<entry key="itemAmenity.delete">
	    <![CDATA[
			DELETE FROM ItemAmenity WHERE Id = ?
	    ]]>
	</entry>
	
	<entry key="itemAmenity.getItemAmenityById">
	    <![CDATA[
			SELECT 
				ItemAmenity.Id, 
				ItemAmenity.ItemId, 
				ItemAmenity.AmenityId, 
				ItemAmenity.Quantity,
				Amenity.Name
			FROM ItemAmenity 
			LEFT OUTER JOIN Amenity ON 
				Amenity.Id = ItemAmenity.AmenityId 
			WHERE 
				ItemAmenity.Id = ?
	    ]]>
	</entry> 
	
	<entry key="itemAmenity.getItemAmenityByItemIdAndAmenityId">
	    <![CDATA[
			SELECT 
				ItemAmenity.Id, 
				ItemAmenity.ItemId, 
				ItemAmenity.AmenityId, 
				ItemAmenity.Quantity,
				Amenity.Name
			FROM ItemAmenity 
			LEFT OUTER JOIN Amenity ON 
				Amenity.Id = ItemAmenity.AmenityId 
			WHERE 
				ItemAmenity.ItemId = ?
				AND ItemAmenity.AmenityId = ?
	    ]]>
	</entry> 
	
	<entry key="itemAmenity.getAllItemAmenitisByItemId">
	    <![CDATA[
			SELECT 
				ItemAmenity.Id, 
				ItemAmenity.ItemId, 
				ItemAmenity.AmenityId, 
				ItemAmenity.Quantity,
				Amenity.Name
			FROM ItemAmenity 
			LEFT OUTER JOIN Amenity ON 
				Amenity.Id = ItemAmenity.AmenityId  
			WHERE 
				ItemAmenity.ItemId = ?
			ORDER BY ItemAmenity.Id DESC
	    ]]>
	</entry>
	
	<entry key="itemAmenity.isAvailable">
	    <![CDATA[
			SELECT
				Id
			FROM ItemAmenity
			WHERE
				(? > 0 AND Id != ? AND AmenityId = ? AND ItemId = ?)
			    OR (? <= 0 AND AmenityId = ? AND ItemId = ?) 
	    ]]>
	</entry>
</properties>